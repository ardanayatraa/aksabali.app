// Tailwind v4 ditangani lewat @tailwindcss/vite di vite.config.js.
// File ini sengaja kosong supaya parent postcss.config.mjs (Next.js / Tailwind v3) ga ke-load.
const config = {
  plugins: {},
};

export default config;
